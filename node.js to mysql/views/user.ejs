<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Table</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .back-btn {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h2>User Table</h2>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Password</th>
                <!-- เพิ่มคอลัมน์อื่นๆ ตามต้องการ -->
            </tr>
        </thead>
        <tbody>
            <% users.forEach(user => { %>
                <tr>
                    <td><%= user.id %></td>
                    <td><%= user.name %></td>
                    <td><%= user.email %></td>
                    <td><%= user.password %></td>
                    <!-- เพิ่มคอลัมน์อื่นๆ ตามต้องการ -->
                </tr>
            <% }); %>
        </tbody>
    </table>
    <a href="/back" class="back-btn">Back to Form</a>
</body>
</html>
